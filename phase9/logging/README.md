# Logging Module

Purpose: Configure structured JSON logging for Phase 9.

Public APIs:
- `configure_logging(config)` — configure root logger using the provided settings object.
- `get_logger(name)` — obtain a module logger.
